export class UploadPage {
    constructor(platform) {
        this.platform = platform;
    }

    render() {
        // Upload page content is static; interactions are bound in the main controller.
    }
}
